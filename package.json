{
  "name": "sequelize-test-setup",
  "description": "set up test database and load fixtures with sequelize",
  "version": "1.0.0",
  "author": "Bound State Software Inc",
  "bugs": "https://github.com/boundstate/sequelize-test-setup/issues",
  "keywords": [
    "sequelize",
    "test",
    "fixtures",
    "migrations"
  ],
  "repository": {
    "type": "git",
    "url": "http://github.com/boundstate/sequelize-test-setup.git"
  },
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "changelog": "conventional-changelog -p angular --context conventional-changelog.json -i CHANGELOG.md -s",
    "version": "npm run changelog && git add CHANGELOG.md"
  },
  "dependencies": {
    "sequelize-fixtures": "^0.10.0",
    "umzug": "^2.2.0"
  },
  "peerDependencies": {
    "sequelize": "^5"
  },
  "devDependencies": {
    "conventional-changelog-cli": "^2.0.21"
  }
}
